{
  "slug": "converting-mlir-to-mlir",
  "title": "Converting MLIR to MLIR",
  "date": "2026-02-28",
  "tags": ["JAX", "StableHLO", "MLIR", "Compilers"],
  "chunks": [
    {
      "type": "text",
      "content": [
        "Waking up for an early morning, brewing up your first coffee for the day, in-between yawns and stretches you might hear a bird chirping just outside your front door. When you hear that chirp, each neuron in your brain has a binary decision to make: do I fire, or do I not fire? Formalized in 1943 by [McCulloch and Pitts](https://en.wikipedia.org/wiki/McCulloch%E2%80%93Pitts_neuron), inputs arrive, and the neuron fires if the weighted sum exceeds a threshold."
      ]
    },
    {
      "type": "text",
      "content": [
        "[ReLU](https://en.wikipedia.org/wiki/Rectifier_(neural_networks)) simulates this real brain mechanism in artificial neural networks. Fire if positive, silence if negative. Modern architectures tend to use GeLU or SiLU, but ReLU is a foundational building block and the one we'll look at.",
        "Every token generated by every LLM request runs an activation function once per feed-forward layer, for every layer in the network. [OpenRouter served over 100 trillion tokens](https://openrouter.ai/state-of-ai) in the 13 months ending November 2025. [Google processes 3.2 quadrillion per month](https://blog.google/innovation-and-ai/sundar-pichai-io-2026/). [DeepSeek-V3](https://github.com/deepseek-ai/DeepSeek-V3) has 61 layers (as a MoE model, only 8 of 256 experts fire per token, but every token still hits all 61 layers). Billions of people, quadrillions of tokens, ~hundreds of layers each, and it's the compiler which decides whether `relu(x + bias)` runs as one kernel or five."
      ]
    },
    {
      "type": "text",
      "content": [
        "With Python at the top and metal at the bottom, it's the compiler glue sitting in the middle. Its job: lower the human-understandable JAX down to GPU-understandable PTX, a TPU executable, an FPGA bitstream, or a TTNN kernel. The compiler maps one program representation to multiple types of hardware, each with their own nuances and tradeoffs. The code below exports two operations to [StableHLO](https://openxla.org/stablehlo) MLIR: a matrix multiply and an elementwise activation."
      ]
    },
    {
      "type": "annotated_code_pair",
      "label": "attention projection",
      "python": {
        "code": "import jax\nimport jax.numpy as jnp\nfrom jax.export import export\n\ndef attention_projection(x, w):\n    return jnp.matmul(x, w)\n\nx = jax.ShapeDtypeStruct((1, 512, 768), jnp.float32)\nw = jax.ShapeDtypeStruct((768, 768), jnp.float32)\n\nexported = export(jax.jit(attention_projection))(x, w)\nprint(exported.mlir_module())",
        "annotations": [
          {
            "term": "jnp.matmul",
            "definition": "Matrix multiply. Core Q/K/V projection op.",
            "why": "One call in Python, one op in the output IR. Clean lowering."
          },
          {
            "term": "jax.jit",
            "definition": "JIT-compiles the function. export() only works on jit-compiled functions.",
            "why": "Without it JAX runs eagerly and there's nothing to export."
          },
          {
            "term": "ShapeDtypeStruct",
            "occurrence": 1,
            "definition": "Shape and dtype only, no data. JAX traces symbolically from these.",
            "why": "Export needs static shapes upfront so the MLIR output can be fully typed at compile time."
          },
          {
            "term": "(1, 512, 768)",
            "definition": "Batch 1, 512 tokens, 768 hidden dims. BERT-base.",
            "why": "512 is the standard max sequence length for BERT-class models; 768 is the standard hidden dim."
          },
          {
            "term": "(768, 768)",
            "definition": "Square weight matrix. Projection preserves embedding size.",
            "why": "In self-attention, Q/K/V projections preserve the embedding size."
          }
        ]
      },
      "mlir": {
        "code": "module @jit_attention_projection {\n  func.func public @main(\n    %arg0: tensor<1x512x768xf32>,\n    %arg1: tensor<768x768xf32>\n  ) -> (tensor<1x512x768xf32>) {\n    %0 = stablehlo.dot_general\n           %arg0, %arg1,\n           contracting_dims = [2] x [0] :\n           (tensor<1x512x768xf32>,\n            tensor<768x768xf32>)\n        -> tensor<1x512x768xf32>\n    return %0 : tensor<1x512x768xf32>\n  }\n}",
        "annotations": [
          {
            "term": "dot_general",
            "definition": "StableHLO's generalized dot product. Handles matmul, batched matmul, tensor contractions.",
            "why": "jnp.matmul compiled to exactly one op. No broadcasting, no reshaping."
          },
          {
            "term": "contracting_dims = [2] x [0]",
            "definition": "Dimensions to contract: dim 2 of input (hidden) against dim 0 of weight (rows).",
            "why": "Explicit dimension spec. The compiler knows exactly what to fuse and parallelize."
          },
          {
            "term": "tensor<1x512x768xf32>",
            "occurrence": 1,
            "definition": "Static tensor type. Shape [1, 512, 768], element type f32.",
            "why": "MLIR is fully typed at compile time. Shape errors surface before any hardware code is generated."
          }
        ]
      }
    },
    {
      "type": "text",
      "content": [
        "The projection lowered cleanly. One Python call, one [StableHLO op](https://github.com/openxla/stablehlo/blob/main/docs/spec.md#dot_general). The elementwise pass is more interesting."
      ]
    },
    {
      "type": "annotated_code_pair",
      "label": "elementwise (bias + relu)",
      "python": {
        "code": "import jax\nimport jax.numpy as jnp\nfrom jax.export import export\n\ndef elementwise(x, bias):\n    return jax.nn.relu(x + bias)\n\nx    = jax.ShapeDtypeStruct((1, 512, 768), jnp.float32)\nbias = jax.ShapeDtypeStruct((768,), jnp.float32)\n\nexported = export(jax.jit(elementwise))(x, bias)\nprint(exported.mlir_module())",
        "annotations": [
          {
            "term": "jax.nn.relu",
            "definition": "f(x) = max(0, x). Clamps negatives to zero.",
            "why": "Introduces non-linearity after the bias add. Without it the whole layer is linear regardless of depth."
          },
          {
            "term": "x + bias",
            "definition": "Per-dimension offset. JAX broadcasts the 768-dim bias across all 512 tokens automatically.",
            "why": "Broadcasting is implicit in Python. In the MLIR output you can see what it actually costs: two ops before the add."
          },
          {
            "term": "(768,)",
            "definition": "One bias value per hidden dim, shared across all tokens.",
            "why": "Bias is positionally invariant. Each of the 768 hidden units gets its own offset, shared across the sequence."
          }
        ]
      },
      "mlir": {
        "code": "module @jit_elementwise {\n  func.func public @main(\n    %arg0: tensor<1x512x768xf32>,\n    %arg1: tensor<768xf32>\n  ) -> (tensor<1x512x768xf32>) {\n    %0 = stablehlo.broadcast_in_dim %arg1,\n           dims = [2] :\n           (tensor<768xf32>)\n        -> tensor<1x1x768xf32>\n    %1 = stablehlo.broadcast_in_dim %0,\n           dims = [0, 1, 2] :\n           (tensor<1x1x768xf32>)\n        -> tensor<1x512x768xf32>\n    %2 = stablehlo.add %arg0, %1\n         : tensor<1x512x768xf32>\n    %3 = call @relu(%2)\n         : (tensor<1x512x768xf32>)\n        -> tensor<1x512x768xf32>\n    return %3 : tensor<1x512x768xf32>\n  }\n\n  func.func private @relu(\n    %arg0: tensor<1x512x768xf32>\n  ) -> tensor<1x512x768xf32> {\n    %cst = stablehlo.constant\n             dense<0.000000e+00> : tensor<f32>\n    %0 = stablehlo.broadcast_in_dim %cst,\n           dims = [] :\n           (tensor<f32>)\n        -> tensor<1x512x768xf32>\n    %1 = stablehlo.maximum %arg0, %0\n         : tensor<1x512x768xf32>\n    return %1 : tensor<1x512x768xf32>\n  }\n}",
        "annotations": [
          {
            "term": "broadcast_in_dim",
            "occurrence": 1,
            "definition": "Expands tensor along new dimensions. Turns 768-dim bias into 1x512x768.",
            "why": "Python's implicit broadcasting made explicit. What looked like one op is actually two broadcasts before the add."
          },
          {
            "term": "stablehlo.add",
            "definition": "Elementwise add across all 393,216 values.",
            "why": "The + in x + bias lands here, after both broadcasts materialize matching shapes."
          },
          {
            "term": "stablehlo.maximum",
            "definition": "ReLU as max(x, zeros_tensor).",
            "why": "relu is sugar in Python. In MLIR it's explicit: compare every value against 0, keep the larger one."
          },
          {
            "term": "func.func private @relu",
            "definition": "JAX preserves function boundaries during tracing. relu becomes its own private function.",
            "why": "Function boundaries are a compilation hint. The compiler can choose to inline or keep it separate."
          }
        ]
      }
    },
    {
      "type": "text",
      "content": [
        "In Python, you can write a single, concise line of code to accomplish something powerful without worrying about the 5 triple kickflips it has to do under the hood to accomplish that something. The kickflips are abstracted away.",
        "One Python expression (`relu(x + bias)`) became two broadcasts, an add, a constant, another broadcast, and a max, when we exported the JAX to StableHLO.",
        "This verbosity is the point - StableHLO makes operations implicit in JAX explicit in IR. Python's `relu` is just `relu` (the thing us humans named it), but in StableHLO it's `max(x, 0)`. It also reveals an optimization opportunity: those five ops: broadcast, broadcast, add, broadcast, and max, each touch every one of the `1 × 512 × 768 = 393,216` values in the tensor independently. A compiler can fuse them into one."
      ]
    },
    {
      "type": "text",
      "content": [
        "With the MLIR in hand, it's time to convert our MLIR to MLIR.",
        "StableHLO is a dialect of MLIR, and now we want to take it another step lower - another step closer to a language a 4090 understands. Where StableHLO describes `what` our JAX wants to accomplish, Linalg dialect describes `how` to accomplish it.",
        "They ship a build script that compiles MLIR from a known-good LLVM commit. The commit hash lives in `build_tools/llvm_version.txt`.",
        "```bash\ngit clone https://github.com/openxla/stablehlo.git\ncd stablehlo\ncat build_tools/llvm_version.txt\n# 1053047a4be7d1fece3adaf5e7597f838058c947\n\ngit clone https://github.com/llvm/llvm-project\ncd llvm-project && git checkout 1053047a4be7d1fece3adaf5e7597f838058c947\ncd ../stablehlo\n\nsh build_tools/build_mlir.sh ../llvm-project ../llvm-build\n```"
      ]
    },
    {
      "type": "text",
      "content": [
        "4,669 compilation units later, MLIR is built. Now configure and build `stablehlo-opt` against it:",
        "```bash\ncmake -GNinja -B build \\\n  -DCMAKE_BUILD_TYPE=Release \\\n  -DMLIR_DIR=../llvm-build/lib/cmake/mlir \\\n  -DLLVM_ENABLE_ASSERTIONS=ON\n\ncmake --build build --target stablehlo-opt\n# [163/163] Linking CXX executable bin/stablehlo-opt\n```"
      ]
    },
    {
      "type": "text",
      "content": [
        "With `stablehlo-opt` built, I pointed it at [the MLIR file](https://github.com/src-kearney/remora/blob/38f93c0d0708dbd654059e54d806754b33a4179b/mlir/stablehlo/simple_attention_elementwise.mlir): a bias add followed by ReLU.",
        "```bash\n./build/bin/stablehlo-opt simple_attention_elementwise.mlir\n```",
        "```mlir\nmodule @jit_elementwise {\n  func.func public @main(%arg0: tensor<1x512x768xf32>, %arg1: tensor<768xf32>)\n      -> tensor<1x512x768xf32> {\n    %0 = stablehlo.broadcast_in_dim %arg1, dims = [2]\n             : (tensor<768xf32>) -> tensor<1x1x768xf32>\n    %1 = stablehlo.broadcast_in_dim %0, dims = [0, 1, 2]\n             : (tensor<1x1x768xf32>) -> tensor<1x512x768xf32>\n    %2 = stablehlo.add %arg0, %1 : tensor<1x512x768xf32>\n    %3 = call @relu(%2) : (tensor<1x512x768xf32>) -> tensor<1x512x768xf32>\n    return %3 : tensor<1x512x768xf32>\n  }\n  func.func private @relu(%arg0: tensor<1x512x768xf32>) -> tensor<1x512x768xf32> {\n    %cst = stablehlo.constant dense<0.000000e+00> : tensor<f32>\n    %0 = stablehlo.broadcast_in_dim %cst, dims = []\n             : (tensor<f32>) -> tensor<1x512x768xf32>\n    %1 = stablehlo.maximum %arg0, %0 : tensor<1x512x768xf32>\n    return %1 : tensor<1x512x768xf32>\n  }\n}\n```",
        "As shown above, those five elementwise ops (broadcast, broadcast, add, broadcast, max) should be fused into a single kernel. Hoping to collapse the chain, I ran `--canonicalize`, a pass that only applies rewrites which are safe to run anywhere: constant folding, algebraic simplification, nothing that changes program size or surprises downstream passes; only to find the exact same output:",
        "```bash\ndiff <(./build/bin/stablehlo-opt simple_attention_elementwise.mlir) \\\n     <(./build/bin/stablehlo-opt simple_attention_elementwise.mlir --canonicalize)\n# (no output)\n```",
        "So, why doesn't this live in `--canonicalize`? Turns out, there's an actual reason. Google's on-device ML team filed an RFC pushing back on MHLO canonicalizers being too aggressive: folding splat broadcasts is fine for server inference, but it increases model size on mobile and hinders removing unnecessary broadcast ops downstream. The [discussion is public](https://groups.google.com/a/openxla.org/g/openxla-discuss/c/VGBIS0kgPKY?pli=1). MLIR's own canonicalization docs reinforce the principle: repeated applications of patterns should converge, and size-affecting transformations can violate that.",
        "The fold does exist, though. `--stablehlo-aggressive-simplification` has an explicit rule for it:",
        "```\nbroadcast_in_dim(broadcast_in_dim(X, [dimsA...]), [dimsB...])\n  -> broadcast_in_dim(X, merge(dimsA, dimsB))\n```",
        "Through folding, we collapse two ops into one - cleaner IR, same semantics. So we want to fold these, right? Seems like a no-brainer.",
        "Folding two broadcasts into one means one fewer op. But the remaining ops still each write a tensor to GPU memory and read it back. _Fusion_, rather than _folding_, eliminates those round-trips. By running all three ops in one kernel, intermediate values never leave the registers. To accomplish fusion, we must lower to Linalg.",
        "```bash\n./build/bin/stablehlo-opt --help | grep linalg\n```",
        "```\n      --convert-elementwise-to-linalg                        -   Convert ElementwiseMappable ops to linalg\n      --convert-linalg-to-affine-loops                       -   Lower the operations from the linalg dialect into affine loops\n      --convert-linalg-to-loops                              -   Lower the operations from the linalg dialect into loops\n      --convert-linalg-to-parallel-loops                     -   Lower the operations from the linalg dialect into parallel loops\n      --convert-linalg-to-std                                -   Convert the operations from the linalg dialect into the Standard dialect\n      --convert-tensor-to-linalg                             -   Convert some Tensor dialect ops to Linalg dialect\n      --linalg-block-pack-matmul                             -   Convert linalg matmul ops to block layout and back\n      --linalg-fold-into-elementwise                         -   Fold transpose ops into elementwise\n      --linalg-fold-unit-extent-dims                         -   Remove unit-extent dimension in Linalg ops on tensors\n      --linalg-fuse-elementwise-ops                          -   Fuse elementwise operations on tensors\n      --linalg-generalize-named-ops                          -   Convert named ops into generic ops\n      --linalg-inline-scalar-operands                        -   Inline scalar operands into linalg generic ops\n      --linalg-morph-ops                                     -   Convert linalg ops between forms\n      --linalg-specialize-generic-ops                        -   Convert generic ops back to named ops\n      --stablehlo-legalize-to-linalg                         -   Legalize StableHLO to LinAlg\n      --tosa-to-linalg                                       -   Lower TOSA to LinAlg on tensors\n      --tosa-to-linalg-named                                 -   Lower TOSA to LinAlg named operations\n      --tosa-to-linalg-pipeline                              -   The default pipeline for converting TOSA operators to linalg\n```",
        "Fortunately, `--stablehlo-legalize-to-linalg` is self-describing: it rewrites `stablehlo.*` ops into `linalg.*` ops the Linalg dialect considers legal. Legalization makes the loops visible, opening the door to fusion.",
        "```bash\n./build/bin/stablehlo-opt simple_attention_elementwise.mlir \\\n  --stablehlo-legalize-to-linalg\n```"
      ]
    },
    {
      "type": "annotated_code",
      "language": "mlir",
      "code": "#map  = affine_map<(d0, d1, d2) -> (d2)>\n#map1 = affine_map<(d0, d1, d2) -> (d0, d1, d2)>\n#map2 = affine_map<(d0, d1, d2) -> (d0, 0, d2)>\n#map3 = affine_map<(d0, d1, d2) -> ()>\nmodule @jit_elementwise {\n  func.func public @main(%arg0: tensor<1x512x768xf32>, %arg1: tensor<768xf32>)\n      -> tensor<1x512x768xf32> {\n    %0 = tensor.empty() : tensor<1x1x768xf32>\n    %1 = linalg.generic {indexing_maps = [#map, #map1],\n                         iterator_types = [\"parallel\", \"parallel\", \"parallel\"]}\n             ins(%arg1 : tensor<768xf32>) outs(%0 : tensor<1x1x768xf32>) {\n    ^bb0(%in: f32, %out: f32):\n      linalg.yield %in : f32\n    } -> tensor<1x1x768xf32>\n    %2 = tensor.empty() : tensor<1x512x768xf32>\n    %3 = linalg.generic {indexing_maps = [#map2, #map1],\n                         iterator_types = [\"parallel\", \"parallel\", \"parallel\"]}\n             ins(%1 : tensor<1x1x768xf32>) outs(%2 : tensor<1x512x768xf32>) {\n    ^bb0(%in: f32, %out: f32):\n      linalg.yield %in : f32\n    } -> tensor<1x512x768xf32>\n    %4 = tensor.empty() : tensor<1x512x768xf32>\n    %5 = linalg.generic {indexing_maps = [#map1, #map1, #map1],\n                         iterator_types = [\"parallel\", \"parallel\", \"parallel\"]}\n             ins(%arg0, %3 : tensor<1x512x768xf32>, tensor<1x512x768xf32>)\n             outs(%4 : tensor<1x512x768xf32>) {\n    ^bb0(%in: f32, %in_0: f32, %out: f32):\n      %7 = arith.addf %in, %in_0 : f32\n      linalg.yield %7 : f32\n    } -> tensor<1x512x768xf32>\n    %6 = call @relu(%5) : (tensor<1x512x768xf32>) -> tensor<1x512x768xf32>\n    return %6 : tensor<1x512x768xf32>\n  }\n  func.func private @relu(%arg0: tensor<1x512x768xf32>) -> tensor<1x512x768xf32> {\n    %cst = arith.constant dense<0.000000e+00> : tensor<f32>\n    %0 = tensor.empty() : tensor<1x512x768xf32>\n    %1 = linalg.generic {indexing_maps = [#map3, #map1],\n                         iterator_types = [\"parallel\", \"parallel\", \"parallel\"]}\n             ins(%cst : tensor<f32>) outs(%0 : tensor<1x512x768xf32>) {\n    ^bb0(%in: f32, %out: f32):\n      linalg.yield %in : f32\n    } -> tensor<1x512x768xf32>\n    %2 = tensor.empty() : tensor<1x512x768xf32>\n    %3 = linalg.generic {indexing_maps = [#map1, #map1, #map1],\n                         iterator_types = [\"parallel\", \"parallel\", \"parallel\"]}\n             ins(%arg0, %1 : tensor<1x512x768xf32>, tensor<1x512x768xf32>)\n             outs(%2 : tensor<1x512x768xf32>) {\n    ^bb0(%in: f32, %in_0: f32, %out: f32):\n      %4 = arith.maximumf %in, %in_0 : f32\n      linalg.yield %4 : f32\n    } -> tensor<1x512x768xf32>\n    return %3 : tensor<1x512x768xf32>\n  }\n}",
      "annotations": [
        {
          "term": "-> (d2)",
          "definition": "Reads bias at the last dimension only. d0 and d1 are ignored — broadcasting as explicit indexing.",
          "why": "What Python made invisible, Linalg makes explicit: one index, loop nest handles repetition."
        },
        {
          "term": "(d0, 0, d2)",
          "definition": "d1 pinned to 0. Loop visits all 512 positions in d1, but this input always reads row 0.",
          "why": "The second broadcast as an indexing rule instead of an op."
        },
        {
          "term": "-> ()",
          "definition": "Empty map. Returns the same scalar for every iteration of the 3D loop nest.",
          "why": "stablehlo.constant lowers to this: one captured value, read identically at every position."
        },
        {
          "term": "tensor.empty()",
          "occurrence": 1,
          "definition": "New output buffer. Five ops = five allocations = five passes over 393,216 floats.",
          "why": "This is the cost the fusion pass eliminates."
        },
        {
          "term": "iterator_types",
          "occurrence": 1,
          "definition": "All three dimensions are parallel. No loop-carried dependencies.",
          "why": "Parallel iterators are the condition for fusion. The pass only merges linalg.generic ops whose iterators are all parallel."
        },
        {
          "term": "^bb0",
          "occurrence": 1,
          "definition": "Per-element scalar body. Runs once per output element.",
          "why": "After fusion, multiple bb0 blocks merge into one, eliminating intermediate buffers."
        },
        {
          "term": "arith.addf",
          "definition": "Float add in the arith dialect. Crossed from stablehlo.add during legalization."
        },
        {
          "term": "arith.constant",
          "definition": "Crossed from stablehlo.constant during legalization. Dialect boundaries are traversed automatically."
        }
      ]
    },
    {
      "type": "text",
      "content": [
        "An observant reader would have noticed `--linalg-fuse-elementwise-ops` in my above `stablehlo-opt --help` command. Let's give it a shot, and cross our fingers we see one single, beautiful, fused op.",
        "```bash\n./build/bin/stablehlo-opt simple_attention_elementwise.mlir \\\n  --stablehlo-legalize-to-linalg \\\n  --linalg-fuse-elementwise-ops\n```"
      ]
    },
    {
      "type": "annotated_code",
      "language": "mlir",
      "code": "#map  = affine_map<(d0, d1, d2) -> (d0, d1, d2)>\n#map1 = affine_map<(d0, d1, d2) -> (d2)>\nmodule @jit_elementwise {\n  func.func public @main(%arg0: tensor<1x512x768xf32>, %arg1: tensor<768xf32>)\n      -> tensor<1x512x768xf32> {\n    %0 = tensor.empty() : tensor<1x512x768xf32>\n    %1 = linalg.generic {indexing_maps = [#map, #map1, #map],\n                         iterator_types = [\"parallel\", \"parallel\", \"parallel\"]}\n             ins(%arg0, %arg1 : tensor<1x512x768xf32>, tensor<768xf32>)\n             outs(%0 : tensor<1x512x768xf32>) {\n    ^bb0(%in: f32, %in_0: f32, %out: f32):\n      %3 = arith.addf %in, %in_0 : f32\n      linalg.yield %3 : f32\n    } -> tensor<1x512x768xf32>\n    %2 = call @relu(%1) : (tensor<1x512x768xf32>) -> tensor<1x512x768xf32>\n    return %2 : tensor<1x512x768xf32>\n  }\n  func.func private @relu(%arg0: tensor<1x512x768xf32>) -> tensor<1x512x768xf32> {\n    %cst = arith.constant 0.000000e+00 : f32\n    %0 = tensor.empty() : tensor<1x512x768xf32>\n    %1 = linalg.generic {indexing_maps = [#map, #map],\n                         iterator_types = [\"parallel\", \"parallel\", \"parallel\"]}\n             ins(%arg0 : tensor<1x512x768xf32>) outs(%0 : tensor<1x512x768xf32>) {\n    ^bb0(%in: f32, %out: f32):\n      %2 = arith.maximumf %in, %cst : f32\n      linalg.yield %2 : f32\n    } -> tensor<1x512x768xf32>\n    return %1 : tensor<1x512x768xf32>\n  }\n}",
      "annotations": [
        {
          "term": "[#map, #map1, #map]",
          "definition": "3D input, 1D bias, 3D output. Broadcasts absorbed into indexing maps — no intermediate buffers.",
          "why": "Broadcasting is just an indexing pattern. The fusion pass folded the broadcast ops into the loop's access maps."
        },
        {
          "term": "tensor.empty()",
          "occurrence": 1,
          "definition": "One allocation, down from three. Intermediate broadcast buffers are gone."
        },
        {
          "term": "arith.maximumf %in, %cst",
          "definition": "ReLU against a scalar constant, not a broadcasted zero tensor. Constant hoisted out of the loop.",
          "why": "The fusion pass simplified @relu internally, removing the zero-tensor materialization."
        },
        {
          "term": "call @relu",
          "definition": "Function boundaries are opaque to the fusion pass. Add and ReLU in separate scopes can't be seen as a producer-consumer pair. --inline removes this boundary.",
          "why": "Inline before fusing."
        }
      ]
    },
    {
      "type": "text",
      "content": [
        "Progress, but not as much dopamine as I was hoping for. The broadcasts collapsed, but the ReLU still lives as a separate op. It turns out, the fusion pass only sees ops within the same function. The add and ReLU lived in separate scopes, so it had no way to know they were a [producer-consumer pair](https://mlir.llvm.org/docs/Dialects/Linalg/#fusion).",
        "Adding `--inline` first copies the ReLU body into @main. Now, the fusion pass can see them together:",
        "```bash\n./build/bin/stablehlo-opt simple_attention_elementwise.mlir \\\n  --stablehlo-legalize-to-linalg \\\n  --inline \\\n  --linalg-fuse-elementwise-ops\n```"
      ]
    },
    {
      "type": "annotated_code",
      "language": "mlir",
      "code": "#map  = affine_map<(d0, d1, d2) -> (d0, d1, d2)>\n#map1 = affine_map<(d0, d1, d2) -> (d2)>\nmodule @jit_elementwise {\n  func.func public @main(%arg0: tensor<1x512x768xf32>, %arg1: tensor<768xf32>)\n      -> tensor<1x512x768xf32> {\n    %cst = arith.constant 0.000000e+00 : f32\n    %0 = tensor.empty() : tensor<1x512x768xf32>\n    %1 = linalg.generic {indexing_maps = [#map, #map1, #map],\n                         iterator_types = [\"parallel\", \"parallel\", \"parallel\"]}\n             ins(%arg0, %arg1 : tensor<1x512x768xf32>, tensor<768xf32>)\n             outs(%0 : tensor<1x512x768xf32>) {\n    ^bb0(%in: f32, %in_0: f32, %out: f32):\n      %2 = arith.addf %in, %in_0 : f32\n      %3 = arith.maximumf %2, %cst : f32\n      linalg.yield %3 : f32\n    } -> tensor<1x512x768xf32>\n    return %1 : tensor<1x512x768xf32>\n  }\n}",
      "annotations": [
        {
          "term": "tensor.empty()",
          "definition": "One allocation, down from 5."
        },
        {
          "term": "[#map, #map1, #map]",
          "definition": "3D input, 1D bias, 3D output. All 5 StableHLO ops collapsed into one loop nest.",
          "why": "#map1 = (d0,d1,d2) -> (d2) broadcasts bias across d0 and d1 for free via the indexing map."
        },
        {
          "term": "arith.addf %in, %in_0",
          "definition": "%in_0 is the bias at d2. Broadcast absorbed into indexing, not a separate op."
        },
        {
          "term": "arith.maximumf %2, %cst",
          "definition": "ReLU inline in the same loop body. relu function is gone — inlined, then fused.",
          "why": "--inline removed the function boundary. --linalg-fuse-elementwise-ops saw add and relu as a producer-consumer pair and merged them."
        }
      ]
    },
    {
      "type": "text",
      "content": [
        "Three off-the-shelf passes: `--stablehlo-legalize-to-linalg` makes ops fusible, `--inline` removes function boundaries, `--linalg-fuse-elementwise-ops` merges adjacent parallel loops.",
        "Before committing to this pipeline, it's worth checking one of the closest things we have to an answer sheet - [IREE](https://github.com/iree-org/iree/blob/1f3f80891cfe3202b737ae1f12a9d3a803ed1c9f/compiler/src/iree/compiler/DispatchCreation/ElementwiseOpFusion.cpp). Turns out, IREE's `ElementwiseOpFusionPass` calls `linalg::populateElementwiseOpsFusionPatterns` - the same function `--linalg-fuse-elementwise-ops` wraps. The difference is what it passes in:",
        "```cpp\nlinalg::ControlFusionFn fuseElementwiseOpsControlFn =\n    [&](OpOperand *fusedOperand) {\n        // Don't fuse if it would exceed backend binding limits\n        if (operands.size() >= kIreeMaxOperandCount) return false;\n        return areFusableAsElementwiseOps(context, fusedOperand, options);\n    };\n\nlinalg::populateElementwiseOpsFusionPatterns(linalgFusionPatterns,\n                                             fuseElementwiseOpsControlFn);\n```",
        "My pipeline naively fuses anything legally fusible. IREE gates every fusion decision against hardware constraints: how many buffers a GPU kernel can accept, whether this crosses a dispatch region boundary, whether this is a broadcast consumer. The mechanism is trivial, the policy is everything."
      ]
    }
  ]
}
